#include <iostream>
using namespace std;

int main() {
   /* const unsigned int N = 10;
    int arr[N]{};

    for (int i = 0; i < N; i++) {
        cin >> arr[i];
    }

    bool a = true;

    for (int i = 1; i < N; i++) {   
        if (arr[i] < arr[i - 1]) {
            a = false;
            break;
        }
    }

    if (a)
        cout << "ok" << endl;
    else
        cout << "ne ok" << endl;

    return 0;*/


    const unsigned int N = 4;
    int arr[N] = { 2, 4, 6, 7 };
    int max = arr[0];
    for (int i = 1; i < N; i++) {
        if (arr[i] > max) {
            max = arr[i];
        }
    }
    cout << max;
}
